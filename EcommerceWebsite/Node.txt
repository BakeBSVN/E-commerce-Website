step 1: create requirements.txt
step 2: create folder "bin"
step 3: in folder "bin" create file setup_eve_window.sh and file setup_eve_ubuntu  copy past code in under 
"
# in file setup_eve_window.sh 
python -m venv venv
activate(){
    . venv/Sctripts/activate
    echo "installing requirements to virtual environment"
    pip install -r requirements.txt
}
activate
"
"
# in file setup_eve_ubuntu
python3 -m venv venv
activate(){
    . venv/bin/activate
    echo "installing requirements to virtual environment"
    pip install -r requirements.txt
}
activate
"
step 4 cereate file ".gitignore"
"
/venv/
*.pyc
"
